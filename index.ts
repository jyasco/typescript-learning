let hasValue = true; //hasValueはtrueかfalseのどちらかの値を持つ
let count: number = 10;
let single: string = 'hello'

let hello: string;
hello = 'hello'

const person = {
  name: {
    first: 'Jack',
    last: 'Smith'
  },
  age: 21
}

console.log(person.name);